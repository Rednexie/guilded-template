module.exports = {
    config: {
      name: "support",
      description: "support",
      usage: "support"
    },
    run: async (client, message, args) => {
      return message.reply("https://guilded.gg/rednexie")
    }
  }
